import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // UI tests serão adicionados quando as telas estiverem estabilizadas
    expect(true, isTrue);
  });
}
